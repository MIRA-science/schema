repos:
  - repo: local
    hooks:
      - id: generate-mira-types
        name: Regenerate MIRA types from schema
        language: system
        entry: make generate
        files: ^mira\.yaml$
        pass_filenames: false
        stages: [pre-commit]
