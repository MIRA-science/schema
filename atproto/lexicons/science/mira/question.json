{
  "lexicon": 1,
  "id": "science.mira.question",
  "description": "MIRA Question — a scientific unknown, addressable by the systematic application of research methods. Maps mira.yaml class Question (mixin NodeSchema). DRAFT.",
  "defs": {
    "main": {
      "type": "record",
      "description": "A research question. The inverse edge 'addressedBy' is not stored; it is derived by the AppView from science.mira.claim.addresses.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["createdAt"],
        "properties": {
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "MIRA dct:created."
          },
          "updatedAt": {
            "type": "string",
            "format": "datetime",
            "description": "MIRA dct:modified."
          },
          "title": {
            "type": "string",
            "maxLength": 5000,
            "description": "MIRA dct:title — short human label (e.g. 'QUE - ...')."
          },
          "description": {
            "type": "string",
            "maxLength": 100000,
            "description": "MIRA dgb:description, inline string form — the question text."
          },
          "descriptionRef": {
            "type": "ref",
            "ref": "science.mira.defs#externalRef",
            "description": "MIRA dgb:description, URI form — content held off-PDS."
          },
          "attributedTo": {
            "type": "ref",
            "ref": "science.mira.defs#agent",
            "description": "MIRA creator, only when it differs from the signing repo DID."
          },
          "container": {
            "type": "ref",
            "ref": "science.mira.defs#externalRef",
            "description": "MIRA has_container — the document/context this node belongs to."
          }
        }
      }
    }
  }
}
