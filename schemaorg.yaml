id: http://schema.org/
name: Schema.org
prefixes:
  schema: http://schema.org/
# This is deliberately a subset

default_prefix: schema
classes:
  CreativeWork:
    class_uri: schema:CreativeWork
    description: (Schema.org) The most generic kind of creative work, including books, movies, photographs, software programs, etc.
